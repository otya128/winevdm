file(GLOB SOURCE *.c *.cpp *.rc)
add_library(system SHARED ${SOURCE} system.def system.drv16.obj)
include_directories(../wine)
add_definitions(-D_X86_ -D__WINESRC__ -D__i386__ -DUSE_COMPILER_EXCEPTIONS -DHAVE_STRNCASECMP -DHAVE__STRNICMP -D_WINTERNL_ -DNtCurrentTeb=NtCurrentTeb__ -Dinline=__inline -DDECLSPEC_HIDDEN= -Dstrncasecmp=_strnicmp)
spec_build(system.drv16 system)
add_dependencies(system convspec generate_asm_system)
target_link_libraries(system libwine winecrt0 wow32 krnl386)
set_target_properties(system PROPERTIES SUFFIX ".drv16")
